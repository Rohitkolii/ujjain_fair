const config = {
  plugins: {
    // Use the standard tailwindcss plugin name so PostCSS can find it
    tailwindcss: {},
    // autoprefixer is commonly used alongside Tailwind
    autoprefixer: {},
  },
};

export default config;
